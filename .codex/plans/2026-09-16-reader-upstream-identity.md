# Upstream resolver identity and availability boundaries

Owner: Editorial PR #12, Codex gap-filling-20260915.

Bind each accepted upstream PR to its repository ID, requiring exact positive integer repository identity and matching PR base identity. Read back repository identity and default branch after the immutable head comparison; movement or replacement invalidates the observation. A missing gh executable returns redacted unmeasured evidence rather than crashing and hiding other upstream obligations.

Eight focused tests pass, including missing executable, mismatched repository identity, and default-branch movement. Diff hygiene passes. No generated files or external state were changed. These checks strengthen the source prerequisite; they do not claim schema or registry landing, runtime adoption, or full-plan completion.
